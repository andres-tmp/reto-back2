server:
  port: 8080

db:
  ip: localhost
  schema: clientes_db

spring:
  profiles:
    active: dev
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${db.ip}/${db.schema}?zeroDateTimeBehavior=convertToNull&serverTimezone=GMT-5
    username: root
    #password: telequipmysql
    password: securePanther-159
    testWhileIdle: true
    validationQuery: SELECT 1